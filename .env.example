# --- سرور (برای صدور خودکار توکن و رفع خطای Token Expired) ---
# اگر این را روی سرور بگذاری، اپ از /api/stream-token توکن تازه می‌گیرد و خطای منقضی شدن برطرف می‌شود.
# از داشبورد Stream: Video app → API Keys → Secret
STREAM_API_SECRET=2bpdhbgp6wunkh2z38efs8xvuxhbskequewt2ec93ntuk5ynyth4tqn52uwfjgqz

# اختیاری روی سرور:
# STREAM_API_KEY=69twsx7eb4ry

# --- فرانت (فقط وقتی سرور STREAM_API_SECRET ندارد) ---
# توکن JWT از داشبورد Stream؛ منقضی می‌شود — ترجیحاً روی سرور STREAM_API_SECRET بگذار.
VITE_STREAM_TOKEN=

# اختیاری (پیش‌فرض‌ها مطابق اپ Stream تو):
# VITE_STREAM_API_KEY=69twsx7eb4ry
# VITE_STREAM_USER_ID=demo-user-PDL5QQZ9
# VITE_STREAM_CALL_ID=demo-call-TDRPw16O

# پورت ثابت سرور (پیش‌فرض: 17443). روی سرور واقعی می‌توانی مثلاً PORT=443 بگذاری.
# PORT=17443

# برای حالت dev: آدرس سرور چت (اگر خالی باشد با serve:https روی همان آدرس وصل می‌شود)
# VITE_CHAT_WS_URL=wss://185.231.112.84:17443
