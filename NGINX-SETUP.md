# راه‌اندازی با HTTPS برای پخش تحت شبکه

اپ با **HTTPS** روی شبکه محلی سرو می‌شود.

## چت همگام (سرور چت)

برای اینکه چت بین همهٔ کاربران سینک شود، سرور چت را **هم‌زمان** با اپ اجرا کن:

```bash
npm run chat:server
```

این سرور روی پورت **۸۷۶۵** گوش می‌دهد. وقتی اپ را با `npm run serve:https` یا از طریق nginx اجرا می‌کنی، کلاینت‌ها به همان host و پورت ۸۷۶۵ وصل می‌شوند و پیام‌ها بین همه سینک می‌شود.

## روش ساده (بدون nginx، بدون مشکل پورت)

اگر nginx خطای «Address already in use» می‌دهد، از سرور Node استفاده کن:

```bash
npm run build
npm run ssl          # یک بار
npm run serve:https
```

سرور یک **پورت آزاد** انتخاب می‌کند و آدرس را در ترمینال چاپ می‌کند، مثلاً:
`https://192.168.100.121:XXXXX` — همان آدرس را در مرورگر باز کن.

---

## روش با nginx

**آدرس اتصال (در صورت موفق بودن nginx):** `https://192.168.100.121:17443`

## پیش‌نیاز

- نصب **nginx** روی سیستم (مک: `brew install nginx`)
- **openssl** (معمولاً روی مک هست)

## مراحل

### ۱. بیلد اپ

```bash
npm run build
```

### ۲. ساخت گواهی SSL (خودامضا)

یک بار اجرا کافی است:

```bash
chmod +x nginx/generate-ssl.sh
./nginx/generate-ssl.sh
```

فایل‌های `nginx/ssl/cert.pem` و `nginx/ssl/key.pem` ساخته می‌شوند.

### ۳. اجرای nginx

از **پوشهٔ پروژه** (همان جایی که `package.json` است):

```bash
mkdir -p nginx/logs
nginx -p "$(pwd)/nginx" -c nginx.conf
```

اگر nginx از قبل در حال اجرا بود، اول متوقفش کن:

```bash
nginx -p "$(pwd)/nginx" -s stop
```

اگر خطای «bind failed (Address already in use)» دیدی، اول **همهٔ nginxها** را ببند:

```bash
killall nginx
```

یا فقط پروسه‌ای که پورت ۱۷۴۴۳ را گرفته:

```bash
lsof -i :17443
# خروجی را ببین؛ ستون دوم PID است. بعد:
kill -9 <PID>
```

بعد دوباره `npm run serve:nginx` را اجرا کن.

### ۴. باز کردن در مرورگر

آدرس اتصال با آی‌پی لپ‌تاپ **192.168.100.121**:

- **روی همین لپ‌تاپ:**  
  **https://localhost:17443**  
  یا **https://192.168.100.121:17443**

- **روی موبایل / لپ‌تاپ داداش (همان وای‌فای):**  
  **https://192.168.100.121:17443**

مرورگر یک بار هشدار «اتصال امن نیست» می‌دهد → **پیشرفته** → **برو به این آدرس** (یا معادلش) را بزن.

بعد از قبول کردن گواهی، دوربین و میکروفون روی همه دستگاه‌ها فعال می‌شوند.

> اگر آی‌پی لپ‌تاپت عوض شد، گواهی را دوباره بساز:  
> `LAN_IP=192.168.100.XXX npm run ssl`  
> بعد nginx را یک بار متوقف و دوباره اجرا کن.

## توقف nginx

```bash
nginx -p "$(pwd)/nginx" -s stop
```

## نکات

- گواهی **خودامضا** است؛ فقط برای استفاده داخل شبکه است و مرورگر یک بار هشدار می‌دهد.
- اگر خواستی روی مک گواهی را «معتبر» کنی تا هشدار نیاید، می‌توانی از **mkcert** استفاده کنی و در nginx همان مسیرها را به cert و key جدید بدهی.
